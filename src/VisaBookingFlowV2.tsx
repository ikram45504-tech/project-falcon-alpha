import { useEffect, useMemo, useState } from "react";
import type {
  BookingTransactionType,
  Party,
  VisaBooking,
  VisaBookingInput,
  VisaBookingLineInput,
  VisaPassengerType,
  VisaPassportDetailInput,
  VisaTransportFleetLineInput,
  VisaType,
  VisaVehicleType,
} from "./db";
import { createVisaBooking, getVisaBookings, updateVisaBooking, voidVisaBooking } from "./db";
import ProgressiveBookingIdentity from "./ProgressiveBookingIdentity";
import { bookingDigitsFromUb, normalizeBookingUb } from "./bookingUb";
import { updateVisaAdditionalDetails } from "./ProgressiveBookingDb";

type Props = { companyId:string; parties:Party[]; transactionType:BookingTransactionType; userId?:string; canCreate?:boolean; canEdit?:boolean; canVoid?:boolean; onBack:()=>void; onChanged?:()=>void|Promise<void> };
type Mode="FORM"|"REGISTER"; type Filter="ALL"|BookingTransactionType;
type VisaRow={rowId:string;passengerType:VisaPassengerType;passengerName:string;visaType:VisaType|"";visaRateSar:string;paxCount:string;roe:string};
type FleetRow={rowId:string;vehicleType:VisaVehicleType;quantity:string;ratePerVehicleSar:string};
type PassportRow={rowId:string;sourceFamilyName:string;passengerType:VisaPassengerType;visaType:VisaType;surname:string;givenName:string;passportNumber:string;nationality:string;dateOfBirth:string;passportIssuance:string;passportExpiry:string};
type ValidityLevel="GUIDANCE"|"VALID"|"REMINDER"|"CLOSE"|"INVALID";
type Validity={level:ValidityLevel;latestEligibleEntry:string;plannedEntry:string;passportExpiry:string;remainingDays:number|null};
type PassportAlert=Validity&{passengerLabel:string};

const visaTypes=[
 {value:"ONLY_UMRAH_VISA" as VisaType,label:"Only Umrah Visa"},
 {value:"UMRAH_VISA_TRANSPORT" as VisaType,label:"Umrah Visa + Transport"},
 {value:"UMRAH_VISA_ONE_WAY_TRANSPORT" as VisaType,label:"Umrah Visa + One-Way Transport"},
 {value:"UMRAH_VISA_FULL_TRANSPORT" as VisaType,label:"Umrah Visa + Full Transport"},
];
const vehicles:[VisaVehicleType,string,number][]=[["CAR","Car",3],["STARIA","Staria",6],["HIACE","Hiace",10],["COASTER","Coaster",16],["BUS","Bus",47]];
function localDate(){const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`}
function n(v:string){const x=Number(v);return Number.isFinite(x)?x:0} function whole(v:string){return Math.max(0,Math.trunc(n(v)))} function money(v:number,c="SAR"){return `${c} ${Number(v||0).toLocaleString("en-PK",{maximumFractionDigits:2})}`} function pkr(v:number){return `Rs ${Number(v||0).toLocaleString("en-PK",{maximumFractionDigits:2})}`}
function capacity(v:VisaVehicleType){return vehicles.find(x=>x[0]===v)?.[2]||0} function vehicleLabel(v:VisaVehicleType){return vehicles.find(x=>x[0]===v)?.[1]||v} function visaLabel(v:VisaType){return visaTypes.find(x=>x.value===v)?.label||v}
function newVisaRow(type:VisaPassengerType="ADULT",roe=""):VisaRow{return{rowId:crypto.randomUUID(),passengerType:type,passengerName:"",visaType:"",visaRateSar:"",paxCount:"",roe}}
function rowHas(r:VisaRow){return Boolean(r.passengerName.trim()||r.visaType||r.visaRateSar.trim()||r.paxCount.trim()||r.roe.trim())} function pax(r:VisaRow){return rowHas(r)?whole(r.paxCount):0}
function needsPrivate(v:VisaType|""){return v==="UMRAH_VISA_ONE_WAY_TRANSPORT"||v==="UMRAH_VISA_FULL_TRANSPORT"} function needsBus(v:VisaType|""){return v==="UMRAH_VISA_FULL_TRANSPORT"}
function suggestedVehicle(p:number):VisaVehicleType{return p<=3?"CAR":p<=6?"STARIA":p<=10?"HIACE":p<=16?"COASTER":"BUS"}
function suggestedFleet(p:number):FleetRow[]{if(p<=0)return[];const out:FleetRow[]=[];let remaining=p;if(remaining>47){const qty=Math.floor(remaining/47);out.push({rowId:crypto.randomUUID(),vehicleType:"BUS",quantity:String(qty),ratePerVehicleSar:""});remaining-=qty*47}if(remaining>0)out.push({rowId:crypto.randomUUID(),vehicleType:suggestedVehicle(remaining),quantity:"1",ratePerVehicleSar:""});return out}
function newPassport():PassportRow{return{rowId:crypto.randomUUID(),sourceFamilyName:"",passengerType:"ADULT",visaType:"ONLY_UMRAH_VISA",surname:"",givenName:"",passportNumber:"",nationality:"",dateOfBirth:"",passportIssuance:"",passportExpiry:""}}
function daysInMonthUtc(y:number,m:number){return new Date(Date.UTC(y,m+1,0)).getUTCDate()} function shiftMonths(iso:string,delta:number){if(!/^\d{4}-\d{2}-\d{2}$/.test(iso))return"";const[y,m,d]=iso.split("-").map(Number);const a=y*12+(m-1)+delta,ty=Math.floor(a/12),tm=((a%12)+12)%12,td=Math.min(d,daysInMonthUtc(ty,tm));return `${ty}-${String(tm+1).padStart(2,"0")}-${String(td).padStart(2,"0")}`}
function daysBetween(a:string,b:string){if(!a||!b)return null;const x=Date.parse(`${a}T00:00:00Z`),y=Date.parse(`${b}T00:00:00Z`);return Number.isFinite(x)&&Number.isFinite(y)?Math.floor((y-x)/86400000):null}
function pretty(iso:string){if(!iso)return"—";const d=new Date(`${iso}T00:00:00`);return Number.isNaN(d.getTime())?iso:d.toLocaleDateString("en-GB",{day:"2-digit",month:"short",year:"numeric"})}
function validity(expiry:string,entry:string):Validity|null{if(!expiry)return null;const latest=shiftMonths(expiry,-6);if(!entry)return{level:"GUIDANCE",latestEligibleEntry:latest,plannedEntry:"",passportExpiry:expiry,remainingDays:null};const seven=shiftMonths(expiry,-7),twelve=shiftMonths(expiry,-12),remainingDays=daysBetween(entry,expiry);if(entry>latest)return{level:"INVALID",latestEligibleEntry:latest,plannedEntry:entry,passportExpiry:expiry,remainingDays};if(entry>seven)return{level:"CLOSE",latestEligibleEntry:latest,plannedEntry:entry,passportExpiry:expiry,remainingDays};if(entry>twelve)return{level:"REMINDER",latestEligibleEntry:latest,plannedEntry:entry,passportExpiry:expiry,remainingDays};return{level:"VALID",latestEligibleEntry:latest,plannedEntry:entry,passportExpiry:expiry,remainingDays}}
function rank(l:ValidityLevel){return l==="INVALID"?4:l==="CLOSE"?3:l==="REMINDER"?2:l==="VALID"?1:0}

export default function VisaBookingFlowV2({companyId,parties,transactionType,userId="",canCreate=true,canEdit=true,canVoid=true,onBack,onChanged}:Props){
 const[mode,setMode]=useState<Mode>("FORM");const[tx,setTx]=useState<BookingTransactionType>(transactionType);const[counterpartyId,setCounterpartyId]=useState("");const[bookingDate,setBookingDate]=useState(localDate());const[ubDigits,setUbDigits]=useState("");const[ubNumber,setUbNumber]=useState("");const[assigned,setAssigned]=useState(false);const[saved,setSaved]=useState(false);const[detailsOpen,setDetailsOpen]=useState(false);
 const[rows,setRows]=useState<VisaRow[]>([newVisaRow()]);const[fleet,setFleet]=useState<FleetRow[]>([]);const[fleetTouched,setFleetTouched]=useState(false);const[busRate,setBusRate]=useState("");
 const[expectedEntryDate,setExpectedEntryDate]=useState("");const[passports,setPassports]=useState<PassportRow[]>([]);const[notes,setNotes]=useState("");const[passportAlert,setPassportAlert]=useState<PassportAlert|null>(null);
 const[entries,setEntries]=useState<VisaBooking[]>([]);const[editingId,setEditingId]=useState<string|null>(null);const[busy,setBusy]=useState(false);const[error,setError]=useState("");const[message,setMessage]=useState("");const[search,setSearch]=useState("");const[filter,setFilter]=useState<Filter>("ALL");
 useEffect(()=>{if(!editingId)setTx(transactionType)},[transactionType,editingId]);useEffect(()=>{void loadEntries("")},[companyId]);
 const summary=useMemo(()=>{const by={ADULT:0,CHILD:0,INFANT:0} as Record<VisaPassengerType,number>;let visaPax=0,privatePax=0,fullBusPax=0,visaSar=0;rows.forEach(r=>{const q=pax(r);if(!q)return;by[r.passengerType]+=q;visaPax+=q;visaSar+=n(r.visaRateSar)*q;if(needsPrivate(r.visaType))privatePax+=q;if(needsBus(r.visaType))fullBusPax+=q});const fleetSar=fleet.reduce((s,f)=>s+n(f.ratePerVehicleSar)*Math.max(1,whole(f.quantity)),0),fleetCapacity=fleet.reduce((s,f)=>s+capacity(f.vehicleType)*Math.max(1,whole(f.quantity)),0),privatePerPax=privatePax?fleetSar/privatePax:0,busSar=fullBusPax?n(busRate)*fullBusPax:0;let convertedPkr=0,unconvertedSar=0;rows.forEach(r=>{const q=pax(r);if(!q)return;let total=n(r.visaRateSar)*q;if(needsPrivate(r.visaType))total+=privatePerPax*q;if(needsBus(r.visaType))total+=n(busRate)*q;const roe=n(r.roe);if(roe>0)convertedPkr+=total*roe;else unconvertedSar+=total});return{...by,visaPax,privatePax,fullBusPax,visaSar,fleetSar,fleetCapacity,privatePerPax,busSar,transportSar:fleetSar+busSar,totalSar:visaSar+fleetSar+busSar,convertedPkr,unconvertedSar}},[rows,fleet,busRate]);
 const hasPrivate=summary.privatePax>0,hasBus=summary.fullBusPax>0;useEffect(()=>{if(!hasPrivate){setFleet([]);setFleetTouched(false)}else if(!fleetTouched&&fleet.length===0)setFleet(suggestedFleet(summary.privatePax))},[hasPrivate,summary.privatePax,fleetTouched,fleet.length]);
 const visible=entries.filter(e=>filter==="ALL"||e.transaction_type===filter);
 async function loadEntries(q=search){try{setEntries(await getVisaBookings(companyId,q))}catch(e){setError(e instanceof Error?e.message:String(e))}}
 function reset(){setTx(transactionType);setCounterpartyId("");setBookingDate(localDate());setUbDigits("");setUbNumber("");setAssigned(false);setSaved(false);setDetailsOpen(false);setRows([newVisaRow()]);setFleet([]);setFleetTouched(false);setBusRate("");setExpectedEntryDate("");setPassports([]);setNotes("");setPassportAlert(null);setEditingId(null);setError("");setMessage("")}
 function assign(formatted:string){setError("");if(!counterpartyId)return setError(tx==="SALE"?"Select a Party / Customer first.":"Select a Vendor / Supplier first.");if(!bookingDate)return setError("Date of Booking is required.");if(!formatted)return setError("Enter a booking number using 1 to 4 digits.");const dup=entries.find(e=>normalizeBookingUb(e.ub_number)===formatted&&(tx==="SALE"?e.transaction_type==="SALE":e.transaction_type==="PURCHASE"&&e.counterparty_id===counterpartyId));if(dup)return setError(tx==="SALE"?`${formatted} already has a Visa Sale booking.`:`This Vendor already has a Visa Purchase booking for ${formatted}.`);setUbNumber(formatted);setAssigned(true);setMessage(`${formatted} is ready. Enter Visa Details & Rates below.`)}
 function addVisaRow(){const roe=[...rows].reverse().find(r=>r.roe.trim())?.roe||"";setRows(v=>[...v,newVisaRow("ADULT",roe)])} function updateRow(id:string,patch:Partial<VisaRow>){setRows(v=>v.map(r=>r.rowId===id?{...r,...patch}:r))} function removeRow(id:string){setRows(v=>{const next=v.filter(r=>r.rowId!==id);return next.length?next:[newVisaRow()]})}
 function addFleet(){setFleetTouched(true);setFleet(v=>[...v,{rowId:crypto.randomUUID(),vehicleType:"CAR",quantity:"1",ratePerVehicleSar:""}])} function updateFleet(id:string,patch:Partial<FleetRow>){setFleetTouched(true);setFleet(v=>v.map(r=>r.rowId===id?{...r,...patch}:r))} function removeFleet(id:string){setFleetTouched(true);setFleet(v=>v.filter(r=>r.rowId!==id))} function useSuggested(){setFleet(suggestedFleet(summary.privatePax));setFleetTouched(true)}
 function rowCalc(r:VisaRow){const q=pax(r);let s=n(r.visaRateSar)*q;if(needsPrivate(r.visaType))s+=summary.privatePerPax*q;if(needsBus(r.visaType))s+=n(busRate)*q;const roe=n(r.roe);return{sar:s,pkr:roe>0?s*roe:0,roe}}
 function lineInputs():VisaBookingLineInput[]{return rows.filter(rowHas).map(r=>({passengerType:r.passengerType,passengerName:r.passengerName.trim(),visaType:r.visaType as VisaType,visaRateSar:n(r.visaRateSar),paxCount:whole(r.paxCount),roe:r.roe.trim()?n(r.roe):null}))}
 function fleetInputs():VisaTransportFleetLineInput[]{return hasPrivate?fleet.map(f=>({vehicleType:f.vehicleType,quantity:Math.max(1,whole(f.quantity)),ratePerVehicleSar:n(f.ratePerVehicleSar)})):[]}
 function passportInputs():VisaPassportDetailInput[]{return passports.map(p=>({sourceFamilyName:p.sourceFamilyName,passengerType:p.passengerType,visaType:p.visaType,surname:p.surname,givenName:p.givenName,passportNumber:p.passportNumber,nationality:p.nationality,dateOfBirth:p.dateOfBirth,passportIssuance:p.passportIssuance,passportExpiry:p.passportExpiry}))}
 function input(includeDetails=true):VisaBookingInput{return{transactionType:tx,counterpartyId,transactionDate:bookingDate,ubNumber,fleet:fleetInputs(),intercityBusRateSar:hasBus?n(busRate):0,expectedEntryDate:includeDetails?expectedEntryDate:"",notes:includeDetails?notes:"",lines:lineInputs(),passports:includeDetails?passportInputs():[]}}
 async function saveCommercial(){if(!assigned)return setError("Create / Assign the Booking UB first.");if(hasPrivate&&summary.fleetCapacity<summary.privatePax)return setError(`Private transport fleet capacity is short by ${summary.privatePax-summary.fleetCapacity} passenger(s). Increase vehicle capacity before saving.`);setBusy(true);setError("");try{if(editingId){await updateVisaBooking(companyId,editingId,input(true),userId);setMessage(`Visa Details & Rates for ${ubNumber} updated.`)}else{const id=await createVisaBooking(companyId,input(false),userId);setEditingId(id);setSaved(true);setMessage(`Visa booking ${ubNumber} saved. Optional Booking & Passenger Details are now available.`)}setSaved(true);await loadEntries(search);await onChanged?.()}catch(e){setError(e instanceof Error?e.message:String(e))}finally{setBusy(false)}}
 async function saveDetails(){if(!editingId)return;setBusy(true);setError("");try{await updateVisaAdditionalDetails(companyId,editingId,{expectedEntryDate,notes,passports:passportInputs()},userId);setMessage(`Visa Booking & Passenger Details for ${ubNumber} saved.`);await loadEntries(search)}catch(e){setError(e instanceof Error?e.message:String(e))}finally{setBusy(false)}}
 function syncPassports(){const desired:PassportRow[]=[];const pools:Record<VisaPassengerType,PassportRow[]>={ADULT:passports.filter(p=>p.passengerType==="ADULT"),CHILD:passports.filter(p=>p.passengerType==="CHILD"),INFANT:passports.filter(p=>p.passengerType==="INFANT")};const used={ADULT:0,CHILD:0,INFANT:0};rows.filter(rowHas).forEach(r=>{for(let i=0;i<pax(r);i++){const type=r.passengerType,existing=pools[type][used[type]++];desired.push(existing?{...existing,sourceFamilyName:r.passengerName.trim(),visaType:(r.visaType||"ONLY_UMRAH_VISA") as VisaType}:{...newPassport(),passengerType:type,sourceFamilyName:r.passengerName.trim(),visaType:(r.visaType||"ONLY_UMRAH_VISA") as VisaType})}});setPassports(desired)}
 const complete=passports.filter(p=>p.surname.trim()&&p.givenName.trim()&&p.passportNumber.trim()&&p.nationality.trim()&&p.dateOfBirth&&p.passportIssuance&&p.passportExpiry).length;
 const validitySummary=useMemo(()=>{let valid=0,warning=0,invalid=0;passports.forEach(p=>{const v=validity(p.passportExpiry,expectedEntryDate);if(!v||v.level==="GUIDANCE")return;if(v.level==="INVALID")invalid++;else if(v.level==="CLOSE"||v.level==="REMINDER")warning++;else valid++});return{valid,warning,invalid}},[passports,expectedEntryDate]);
 function passengerLabel(p:PassportRow,i:number){return `${p.surname.trim()} ${p.givenName.trim()}`.trim()||p.sourceFamilyName||`Passenger ${i+1}`}
 function maybeAlert(p:PassportRow,i:number,expiry:string,entry=expectedEntryDate){const v=validity(expiry,entry);if(!v)return;if(!entry){const today=localDate(),next=shiftMonths(today,12);if(expiry>=today&&expiry<=next)setPassportAlert({...v,passengerLabel:passengerLabel({...p,passportExpiry:expiry},i)});return}if(["REMINDER","CLOSE","INVALID"].includes(v.level))setPassportAlert({...v,passengerLabel:passengerLabel({...p,passportExpiry:expiry},i)})}
 function updatePassport(id:string,patch:Partial<PassportRow>){setPassports(v=>v.map(p=>p.rowId===id?{...p,...patch}:p))} function changeEntry(value:string){setExpectedEntryDate(value);if(!value)return;const alerts=passports.map((p,i)=>{const v=validity(p.passportExpiry,value);return v?{...v,passengerLabel:passengerLabel(p,i)}:null}).filter((x):x is PassportAlert=>!!x).sort((a,b)=>rank(b.level)-rank(a.level));if(alerts.length&&alerts[0].level!=="VALID")setPassportAlert(alerts[0])}
 function badge(p:PassportRow){const v=validity(p.passportExpiry,expectedEntryDate);if(!v)return null;if(!expectedEntryDate)return <small className="visa11d-validity guidance">Latest eligible entry: {pretty(v.latestEligibleEntry)}</small>;if(v.level==="INVALID")return <small className="visa11d-validity invalid">✕ Under 6 months at planned entry</small>;if(v.level==="CLOSE")return <small className="visa11d-validity close">⚠ Close to 6-month minimum</small>;if(v.level==="REMINDER")return <small className="visa11d-validity reminder">⚠ Passport validity reminder</small>;return <small className="visa11d-validity valid">✓ Valid for planned entry</small>}
 function edit(e:VisaBooking){if(!canEdit||e.status!=="ACTIVE")return;setTx(e.transaction_type);setCounterpartyId(e.counterparty_id);setBookingDate(e.transaction_date);setUbNumber(e.ub_number);setUbDigits(bookingDigitsFromUb(e.ub_number));setAssigned(true);setSaved(true);setDetailsOpen(false);setRows(e.lines.map(l=>({rowId:crypto.randomUUID(),passengerType:l.passenger_type,passengerName:l.passenger_name,visaType:l.visa_type,visaRateSar:String(l.visa_rate_sar||""),paxCount:String(l.pax_count||""),roe:l.roe>0?String(l.roe):""})));setFleet(e.fleet?.length?e.fleet.map(f=>({rowId:crypto.randomUUID(),vehicleType:f.vehicle_type,quantity:String(f.quantity),ratePerVehicleSar:String(f.rate_per_vehicle_sar||"")})):[]);setFleetTouched(true);setBusRate(e.intercity_bus_rate_sar>0?String(e.intercity_bus_rate_sar):"");setExpectedEntryDate(e.expected_entry_date||"");setPassports((e.passports||[]).map(d=>({rowId:crypto.randomUUID(),sourceFamilyName:d.source_family_name,passengerType:d.passenger_type,visaType:d.visa_type,surname:d.surname||"",givenName:d.given_name||d.passenger_name||"",passportNumber:d.passport_number,nationality:d.nationality,dateOfBirth:d.date_of_birth,passportIssuance:d.passport_issuance||"",passportExpiry:d.passport_expiry})));setNotes(e.notes||"");setPassportAlert(null);setEditingId(e.id);setMode("FORM");setMessage(`Editing Visa booking ${e.ub_number}. Identity is locked; Section 02 and Section 03 save independently.`);window.scrollTo({top:0,behavior:"smooth"})}
 async function voidEntry(e:VisaBooking){if(!canVoid||e.status!=="ACTIVE"||busy)return;if(!window.confirm(`Void Visa booking ${e.ub_number}?`))return;setBusy(true);try{await voidVisaBooking(companyId,e.id,userId);await loadEntries(search);await onChanged?.();setMessage(`Visa booking ${e.ub_number} voided.`)}catch(err){setError(err instanceof Error?err.message:String(err))}finally{setBusy(false)}}
 function alertModal(){if(!passportAlert)return null;const a=passportAlert,isInvalid=a.level==="INVALID",title=isInvalid?"Passport Does Not Meet Validity Requirement":a.level==="CLOSE"?"Passport Expiry Warning":a.level==="REMINDER"?"Passport Validity Reminder":a.level==="GUIDANCE"?"Travel Date Guidance":"Passport Validity Check";return <div className="visa11d-modal-backdrop" onMouseDown={e=>e.currentTarget===e.target&&setPassportAlert(null)}><div className={`visa11d-validity-modal ${a.level.toLowerCase()}`}><button className="visa11d-modal-close" onClick={()=>setPassportAlert(null)}>×</button><div className="visa11d-modal-icon">{isInvalid?"!":"i"}</div><div><small className="visa11d-modal-kicker">PASSPORT TRAVEL ELIGIBILITY</small><h3>{title}</h3><p className="visa11d-passenger-label">{a.passengerLabel}</p></div><div className="visa11d-modal-grid"><div><small>Planned Entry into KSA</small><b>{a.plannedEntry?pretty(a.plannedEntry):"Not entered"}</b></div><div><small>Passport Expiry</small><b>{pretty(a.passportExpiry)}</b></div><div><small>Latest Eligible Entry Date</small><b>{pretty(a.latestEligibleEntry)}</b></div><div><small>Validity Remaining at Entry</small><b>{a.remainingDays==null?"—":`${a.remainingDays} days`}</b></div></div><p className="visa11d-modal-message">{isInvalid?"This passport does not meet the 6-month validity rule for the selected entry date.":"Review the passport validity against the planned entry date before processing."}</p><p className="visa11d-modal-disclaimer">Passport validity is one eligibility check only. Visa approval and entry remain subject to the relevant Saudi authorities.</p><button className="visa11d-modal-ok" onClick={()=>setPassportAlert(null)}>OK</button></div></div>}

 if(mode==="REGISTER")return <section className="booking-entry-screen visa11-screen"><div className="booking-screen-toolbar"><button className="booking-back-button" onClick={()=>setMode("FORM")}>← Back to Visa Booking</button><span className="booking-foundation-badge active-engine">VISA REGISTER</span></div><div className="visa11-register-controls"><div className="package-register-filter-tabs">{(["ALL","SALE","PURCHASE"] as Filter[]).map(x=><button key={x} className={filter===x?"active":""} onClick={()=>setFilter(x)}>{x}</button>)}</div><div className="search-box package-search"><input value={search} onChange={async e=>{setSearch(e.target.value);await loadEntries(e.target.value)}} placeholder="Search UB, Party/Vendor, Passport, Visa Type..."/></div></div><div className="party-table-wrap"><table className="party-table visa11-register-table"><thead><tr><th>Date</th><th>UB</th><th>Type</th><th>Party / Vendor</th><th>Visa Rows</th><th>Transport Fleet</th><th>Passengers</th><th>Total SAR</th><th>Total PKR</th><th>Status</th><th>Actions</th></tr></thead><tbody>{visible.map(e=><tr key={e.id} className={e.status==="VOID"?"void-row":""}><td>{e.transaction_date}</td><td><b>{e.ub_number}</b></td><td>{e.transaction_type}</td><td>{e.counterparty_name}</td><td>{e.lines.map(l=><div key={l.id}><b>{l.passenger_name}</b> · {l.pax_count} {l.passenger_type} · {visaLabel(l.visa_type)}</div>)}</td><td>{e.fleet?.length?e.fleet.map(f=><div key={f.id}>{f.quantity} × {vehicleLabel(f.vehicle_type)} · {money(f.line_total_sar)}</div>):"No private fleet"}</td><td>{(e.passports||[]).length}/{e.lines.reduce((s,l)=>s+l.pax_count,0)}</td><td>{money(e.total_sar)}</td><td>{pkr(e.total_pkr)}</td><td>{e.status}</td><td><div className="row-actions"><button disabled={!canEdit||e.status!=="ACTIVE"} onClick={()=>edit(e)}>Edit / Details</button><button disabled={!canVoid||e.status!=="ACTIVE"} onClick={()=>void voidEntry(e)}>Void</button></div></td></tr>)}</tbody></table></div></section>;

 return <section className="booking-entry-screen visa11-screen package14-page"><div className="booking-screen-toolbar visa11-toolbar"><button className="booking-back-button" onClick={onBack}>← Back to Booking Services</button><div className="visa11-toolbar-right"><span className={`direction-badge ${tx==="SALE"?"sale":"purchase"}`}>{tx==="SALE"?"SALE TO PARTY":"PURCHASE FROM VENDOR / SUPPLIER"}</span><button className="visa11-register-button" onClick={()=>{setMode("REGISTER");void loadEntries(search)}}>Visa Booking Register</button></div></div><div className="visa11-title-row"><span className="eyebrow blue">VISA BOOKING</span><h2>{saved?`Visa Booking — ${ubNumber}`:"New Visa Booking"}</h2><p>Create the UB first, save Visa calculations second, then complete optional passenger/passport information.</p></div>{message&&<div className="alert success">{message}</div>}{error&&<div className="alert error">{error}</div>}
 <ProgressiveBookingIdentity companyId={companyId} userId={userId} transactionType={tx} parties={parties} counterpartyId={counterpartyId} onCounterpartyChange={setCounterpartyId} bookingDate={bookingDate} onBookingDateChange={setBookingDate} ubDigits={ubDigits} onUbDigitsChange={setUbDigits} ubNumber={ubNumber} assigned={assigned} saved={saved} onAssign={assign} onEditHeader={()=>{setAssigned(false);setMessage("")}} onAccountsChanged={onChanged} onError={setError} onMessage={setMessage} serviceLabel="Visa" />
 {assigned&&<section className="visa11-card"><div className="visa11-passenger-head"><div className="visa11-section-head no-margin"><span>2</span><b>VISA DETAILS & RATES</b><small>Commercial / accounting Visa data under {ubNumber}</small></div><div className="visa11-add-buttons"><button onClick={addVisaRow}>+ Add Visa Row</button></div></div><div className="visa11-combined-note"><b>Umrah Visa + Transport:</b> enter the combined Visa + Transport amount directly in Visa Rate. No separate fleet is used for that type.</div><div className="visa11-table-wrap"><table className="visa11-rate-table"><thead><tr><th>SR</th><th>Passenger / Family Head</th><th>Type</th><th>Visa Type</th><th>Visa Rate SAR</th><th>No. Pax</th><th>ROE</th><th>Total SAR</th><th>Total PKR</th><th></th></tr></thead><tbody>{rows.map((r,i)=>{const c=rowCalc(r);return <tr key={r.rowId}><td>{i+1}</td><td><input value={r.passengerName} onChange={e=>updateRow(r.rowId,{passengerName:e.target.value})}/></td><td><select value={r.passengerType} onChange={e=>updateRow(r.rowId,{passengerType:e.target.value as VisaPassengerType})}><option value="ADULT">Adult</option><option value="CHILD">Child</option><option value="INFANT">Infant</option></select></td><td><select value={r.visaType} onChange={e=>updateRow(r.rowId,{visaType:e.target.value as VisaType})}><option value="">Select Visa Type</option>{visaTypes.map(v=><option key={v.value} value={v.value}>{v.label}</option>)}</select></td><td><input type="number" value={r.visaRateSar} onChange={e=>updateRow(r.rowId,{visaRateSar:e.target.value})}/></td><td><input type="number" value={r.paxCount} onChange={e=>updateRow(r.rowId,{paxCount:e.target.value})}/></td><td><input type="number" value={r.roe} onChange={e=>updateRow(r.rowId,{roe:e.target.value})} placeholder="Optional"/></td><td><b>{money(c.sar)}</b></td><td><b>{c.roe>0?pkr(c.pkr):"PKR —"}</b></td><td><button className="visa11-remove" onClick={()=>removeRow(r.rowId)}>×</button></td></tr>})}</tbody></table></div>
 {hasPrivate&&<div className="visa11c-transport-layout"><div className="visa11c-suggestion"><div><small>SUGGESTED PRIVATE FLEET</small><b>{summary.privatePax<=47?`1 × ${vehicleLabel(suggestedVehicle(summary.privatePax))}`:"Multiple Vehicles"}</b><span>Required Pax: {summary.privatePax}</span></div><button onClick={useSuggested}>Use Suggested Fleet</button></div><div className="visa11c-fleet"><div className="visa11c-fleet-head"><b>PRIVATE / AIRPORT TRANSPORT FLEET</b><button onClick={addFleet}>+ Add Vehicle</button></div><table><thead><tr><th>Vehicle</th><th>Qty</th><th>Capacity</th><th>Total Capacity</th><th>Rate / Vehicle SAR</th><th>Sub Total</th><th></th></tr></thead><tbody>{fleet.map(f=><tr key={f.rowId}><td><select value={f.vehicleType} onChange={e=>updateFleet(f.rowId,{vehicleType:e.target.value as VisaVehicleType})}>{vehicles.map(v=><option key={v[0]} value={v[0]}>{v[1]}</option>)}</select></td><td><input type="number" value={f.quantity} onChange={e=>updateFleet(f.rowId,{quantity:e.target.value})}/></td><td>{capacity(f.vehicleType)}</td><td>{capacity(f.vehicleType)*Math.max(1,whole(f.quantity))}</td><td><input type="number" value={f.ratePerVehicleSar} onChange={e=>updateFleet(f.rowId,{ratePerVehicleSar:e.target.value})}/></td><td>{money(n(f.ratePerVehicleSar)*Math.max(1,whole(f.quantity)))}</td><td><button className="visa11-remove" onClick={()=>removeFleet(f.rowId)}>×</button></td></tr>)}</tbody></table><div className={`visa11c-capacity ${summary.fleetCapacity>=summary.privatePax?"ok":"warn"}`}><b>Required Pax: {summary.privatePax}</b><b>Fleet Capacity: {summary.fleetCapacity}</b><span>{summary.fleetCapacity>=summary.privatePax?`Capacity sufficient · ${summary.fleetCapacity-summary.privatePax} spare seats`:`Capacity short by ${summary.privatePax-summary.fleetCapacity} passenger(s) — booking cannot be saved`}</span></div></div>{hasBus&&<div className="visa11c-bus"><b>INTER-CITY BUS TRANSPORT</b><span>Applicable Full Transport Pax: <strong>{summary.fullBusPax}</strong></span><label>Rate SAR / Pax<input type="number" value={busRate} onChange={e=>setBusRate(e.target.value)}/></label><div>Bus Total <b>{money(summary.busSar)}</b></div></div>}</div>}
 <div className="visa11-count-summary"><div><small>ADULTS</small><b>{summary.ADULT}</b></div><div><small>CHILDREN</small><b>{summary.CHILD}</b></div><div><small>INFANTS</small><b>{summary.INFANT}</b></div><div><small>TOTAL VISA PAX</small><b>{summary.visaPax}</b></div><div><small>PRIVATE TRANSPORT PAX</small><b>{summary.privatePax}</b></div><div><small>FULL TRANSPORT PAX</small><b>{summary.fullBusPax}</b></div></div><section className="visa11-financial-summary"><div><small>VISA TOTAL SAR</small><b>{money(summary.visaSar)}</b></div><div><small>TRANSPORT TOTAL SAR</small><b>{money(summary.transportSar)}</b></div><div className="grand-sar"><small>GRAND TOTAL SAR</small><b>{money(summary.totalSar)}</b></div><div className="grand-pkr"><small>GRAND TOTAL PKR</small><b>{pkr(summary.convertedPkr)}</b>{summary.unconvertedSar>0&&<span>ROE pending for {money(summary.unconvertedSar)}</span>}</div></section><div className="package14-commercial-actions">{saved&&<button className="secondary" onClick={reset}>+ New Visa Booking</button>}<button className="primary" disabled={busy||(!editingId&&!canCreate)||(!!editingId&&!canEdit)} onClick={()=>void saveCommercial()}>{busy?"Saving...":editingId?`Update Visa Details & Rates — ${ubNumber}`:`Save Visa Booking — ${ubNumber}`}</button></div></section>}
 {saved&&editingId&&<section className={`package14-additional ${detailsOpen?"open":"closed"}`}><button className="package14-additional-toggle" onClick={()=>setDetailsOpen(v=>!v)}><span className="package14-step-purple">03</span><div><b>BOOKING & PASSENGER DETAILS — {ubNumber}</b><small>Optional passport information and booking notes. This section does not change Visa calculations.</small></div><span className="package14-optional">OPTIONAL</span><strong>{detailsOpen?"Close Details ▲":"+ Open Details ▼"}</strong></button>{detailsOpen&&<div className="package14-additional-body"><div className="visa11d-entry-row"><label>Expected Entry Date into Saudi Arabia<input type="date" value={expectedEntryDate} onChange={e=>changeEntry(e.target.value)}/></label><div className="visa11d-entry-help"><b>Passport Travel Eligibility</b><span>Expiry is compared against planned Saudi entry using a 6-month validity margin.</span></div></div><div className="visa11-passenger-head"><div className="visa11-section-head no-margin"><b>PASSENGER / PASSPORT DETAILS</b></div><div className="visa11-add-buttons"><button onClick={syncPassports}>Generate / Sync from Visa Pax</button><button onClick={()=>setPassports(v=>[...v,newPassport()])}>+ Add Passenger</button></div></div><div className="visa11c-passport-stats visa11d-passenger-stats"><span>Total Visa Pax <b>{summary.visaPax}</b></span><span>Passenger Records <b>{passports.length}</b></span><span>Completed <b>{complete}/{summary.visaPax}</b></span>{expectedEntryDate&&<span className="valid-count">Valid <b>{validitySummary.valid}</b></span>}{expectedEntryDate&&validitySummary.warning>0&&<span className="warning-count">Check <b>{validitySummary.warning}</b></span>}{expectedEntryDate&&validitySummary.invalid>0&&<span className="invalid-count">Under 6 Months <b>{validitySummary.invalid}</b></span>}</div><div className="visa11-table-wrap"><table className="visa11c-passport-table visa11d-passenger-table"><thead><tr><th>SR</th><th>Surname</th><th>Given Name</th><th>Passport #</th><th>Nationality</th><th>DOB</th><th>Passport Issuance</th><th>Passport Expiry</th></tr></thead><tbody>{passports.map((p,i)=><tr key={p.rowId}><td><b>{i+1}</b></td><td><input value={p.surname} onChange={e=>updatePassport(p.rowId,{surname:e.target.value.toUpperCase()})}/></td><td><input value={p.givenName} onChange={e=>updatePassport(p.rowId,{givenName:e.target.value.toUpperCase()})}/></td><td><input value={p.passportNumber} onChange={e=>updatePassport(p.rowId,{passportNumber:e.target.value.toUpperCase()})}/></td><td><input value={p.nationality} onChange={e=>updatePassport(p.rowId,{nationality:e.target.value})}/></td><td><input type="date" value={p.dateOfBirth} onChange={e=>updatePassport(p.rowId,{dateOfBirth:e.target.value})}/></td><td><input type="date" value={p.passportIssuance} onChange={e=>updatePassport(p.rowId,{passportIssuance:e.target.value})}/></td><td><input type="date" value={p.passportExpiry} onChange={e=>{const expiry=e.target.value;updatePassport(p.rowId,{passportExpiry:expiry});maybeAlert(p,i,expiry)}}/>{badge(p)}</td></tr>)}</tbody></table></div><div className="visa11d-booking-notes"><div><b>BOOKING NOTES</b><small>Visa processing, missing documents, customer instructions or internal remarks.</small></div><textarea rows={4} value={notes} onChange={e=>setNotes(e.target.value)}/></div><div className="package14-details-actions"><span>Optional — can be completed later.</span><button className="primary" disabled={busy||!canEdit} onClick={()=>void saveDetails()}>{busy?"Saving...":`Save Visa Passenger Details — ${ubNumber}`}</button></div></div>}</section>}
 {!assigned&&<div className="package14-next-step">Create / Assign a Booking UB to unlock Visa Details & Rates.</div>}{assigned&&!saved&&<div className="package14-next-step">Save Section 02 to activate the Visa booking and unlock Optional Booking & Passenger Details.</div>}{alertModal()}
 </section>;
}
